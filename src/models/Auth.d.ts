type GetNonceType = {
  address: string;
};

type GetNonceData = {
  address: string;
  nonce: string;
};

type GetTokenType = {
  address: string;
  signature: string;
};

type GetTokenData = {
  accessToken: string;
};
